# ============================================================================
# A100 GPU 립싱크 서버 - 환경 변수 설정
# ============================================================================
# 이 파일을 .env로 복사하여 사용하세요: cp .env.a100 .env
# API 키는 실제 값으로 교체하세요.
# ============================================================================

# ============================================================================
# 서버 설정
# ============================================================================

SERVER_HOST=0.0.0.0
SERVER_PORT=8000
DEBUG=false
LOG_LEVEL=INFO

# ============================================================================
# 외부 API 키 (실제 키로 교체 필요)
# ============================================================================

# OpenAI (LLM) - Fallback용
OPENAI_API_KEY=sk-your-openai-api-key-here

# LLM API Server
LLM_API_URL=https://api.mindprep.co.kr/v1/chat/completions
LLM_API_KEY=your-llm-api-key-here
LLM_MODEL=vllm-qwen3-30b-a3b

# Deepgram (STT) - 선택사항
DEEPGRAM_API_KEY=your-deepgram-api-key-here

# ElevenLabs (TTS) - 선택사항
ELEVENLABS_API_KEY=your-elevenlabs-api-key-here

# Daily.co (WebRTC) - 필요 시
DAILY_API_KEY=
DAILY_ROOM_URL=

# ============================================================================
# AI 모델 설정
# ============================================================================

# STT
STT_PROVIDER=whisper
STT_MODEL=base
STT_LANGUAGE=ko

# LLM
LLM_PROVIDER=custom
LLM_TEMPERATURE=0.7
LLM_MAX_TOKENS=150

# TTS - EdgeTTS (무료)
TTS_PROVIDER=edge
TTS_EDGE_VOICE=ko-KR-SunHiNeural

# Avatar
AVATAR_FPS=25
AVATAR_RESOLUTION=512
AVATAR_BATCH_SIZE=4

# ============================================================================
# GPU 및 최적화 설정 (A100 80GB)
# ============================================================================

CUDA_VISIBLE_DEVICES=0
PRECISION_MODE=fp16
ENABLE_TENSORRT=true
ENABLE_BATCHING=true
BATCH_SIZE=4
ENABLE_CACHE=true
MAX_CACHE_SIZE_MB=4096

# ============================================================================
# Redis (선택사항)
# ============================================================================

REDIS_URL=redis://localhost:6379
ENABLE_REDIS=false

# ============================================================================
# 보안 설정
# ============================================================================

CORS_ORIGINS=*
RATE_LIMIT_PER_MINUTE=120

# ============================================================================
# TTS 서버 (같은 A100에서 실행 시 localhost)
# ============================================================================

# CosyVoice (TTS) - 같은 서버에서 실행
COSYVOICE_API_URL=http://localhost:5001

# Qwen3-TTS (음성 클론 TTS) - 같은 서버에서 실행
QWEN3_TTS_API_URL=http://localhost:8001
QWEN3_TTS_REF_AUDIO=/path/to/reference_audio.mp3
QWEN3_TTS_REF_TEXT=안녕하세요. 오늘 저의 면접에 참석해 주셔서 감사합니다. 저는 박현준 팀장입니다.

# ============================================================================
# 외부 접근 URL 설정
# ============================================================================

# 립싱크 서버의 외부 접근 URL (실제 도메인으로 변경)
LIPSYNC_BASE_URL=https://avatar.mindprep.co.kr
